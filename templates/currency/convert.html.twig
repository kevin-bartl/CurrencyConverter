<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <meta charset="UTF-8">
    <title>Currency Converter</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container" style="width:400px;">
        <h1>Currency Converter</h1>
        <form method="POST">
            <div class="row">
                <div class="col-6"><label for="base">Base currency</label></div>
                <div class="col-6"><input type="text" size="4" maxlength="3" name="base" value="{{ app.request.get('base') }}" placeholder="e.g. USD" /></div>
            </div>
            <div class="row">
                <div class="col-6"><label for="target">Target currency</label></div>
                <div class="col-6"><input type="text" size="4" maxlength="3" name="target" value="{{ app.request.get('target') }}" placeholder="e.g. EUR" /></div>
            </div>
            <div class="row">
                <div class="col-6"><label for="amount">Amount</label></div>
                <div class="col-6"><input type="number" min="0" step="0.01" name="amount" value="{{ app.request.get('amount') }}" /></div>
            </div>
            <div class="row">
                <div class="col-6"><input type="submit" value="Convert"/></div>
                <div class="col-6">
                    <input disabled value="{% if converted is defined %}{{ converted }}{% endif %}"/>
                </div>
            </div>
            <div class="row">
                {%  if errorMessage is defined %}<div class="alert alert-danger">{{ errorMessage }}</div>{% endif %}
            </div>
        </form>
        <br/>
        <footer>powered by fixer.io</footer>
    </div>
</body>
</html>
